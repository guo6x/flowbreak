# FlowBreak 隐私政策

生效日期：2026-07-11

FlowBreak 是一款完全本地运行的 Android 数字健康工具。我们不要求注册账号，不展示广告，不接入第三方统计 SDK，也不把使用数据上传到服务器。

## 本地处理的数据

- 用户主动选择的受限应用包名。
- 目标应用的使用时长聚合。
- 阻断尝试、休息完成、休息后是否回到目标应用、成功拉回结果和紧急解锁事件。
- 用户每天主动选择的“有帮助 / 一般 / 没帮助”反馈。
- 用户设置、积分、成就和连续完成天数。

这些数据保存在设备的 SharedPreferences 和 Room 数据库中，仅用于提供应用阻断、休息解锁和本地统计。

## Android 权限用途

- 使用情况访问：识别前台应用并计算所选应用的连续使用时长；不会读取应用内容。
- 悬浮窗：在达到阻断条件时显示覆盖页。
- 通知和前台服务：保持检测运行并显示必要状态。
- 电池优化豁免：降低部分厂商系统杀死后台检测的概率。
- 无障碍服务（仅国内版、用户主动开启）：仅监听窗口切换事件。在命中用户所选、且已进入阻断状态的应用时返回桌面并显示阻断页。服务不读取、记录或上传页面内容。Google Play 版不声明或使用无障碍服务。

## 数据控制

用户可在应用内导出完整 JSON 数据（包含设置、使用聚合、每日统计、事件和进度），也可单独导出脱敏诊断。脱敏诊断仅包含版本、渠道、状态机状态、服务心跳、权限结果和本地记录数量，不包含姓名、受限应用包名、具体使用明细或休息活动内容。用户可以永久清除全部本地数据与导出/崩溃缓存；卸载应用也会删除应用私有目录中的数据。

## 联系与变更

隐私政策变更会记录在项目 `CHANGELOG.md`。当前项目尚未配置公开支持邮箱和隐私政策 URL；在任何公开商店发布前，必须补充这两项联系信息。
