import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.flowbreak.app',
  appName: 'FlowBreak',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
